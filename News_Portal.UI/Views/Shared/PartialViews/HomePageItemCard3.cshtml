@using News_Portal.Core.DTO.News
@model HomePageNewsToShowDTO
@using System.Globalization
@{
    var lang = Context.Request.Cookies["Language"];
    if (lang == null) lang = Languages.Bangla.ToString();
    bool isBangla = (lang == Languages.Bangla.ToString()) ? true : false;
}
@{
    var content = Model.NewsContent ?? string.Empty;
    var snippet = content;
    var dateText = isBangla ? Model.PublishedDate.GetBengaliDate() : Model.PublishedDate.ToString("MMM dd, yyyy");
    var category = isBangla ? Model.NewsType.GetDisplayName() : Model.NewsType.ToString();
}

@functions {
    private string GetCategoryIcon(string category)
    {
        return category.ToLower() switch
        {
            "politics" => "🏛️",
            "sports" => "⚽",
            "technology" => "💻",
            "entertainment" => "🎬",
            "business" => "💼",
            "health" => "🏥",
            "science" => "🔬",
            "world" => "🌍",
            _ => "📰"
        };
    }

    
}
<a asp-action="Details" asp-controller="Home" asp-route-newsId="@Model.NewsId" class="news-card-compact text-decoration-none d-block">

<article class="modern-news-card" data-category="@category">
    <div class="card-media">
        @if (!string.IsNullOrWhiteSpace(Model.TitleImageUrl))
        {
            <img src="@Model.TitleImageUrl"
                 alt="@Model.NewsTitle"
                 class="card-image"
                 loading="lazy" />
            @* <div class="image-overlay">
                <span class="category-badge badge-@category">
                    @GetCategoryIcon(category) @Model.NewsType.ToString()
                </span>
            </div> *@
        }
        else
        {
            <div class="card-placeholder">
                <div class="placeholder-content">
                    <span class="placeholder-icon">@GetCategoryIcon(category)</span>
                        <span class="placeholder-text">@(isBangla? Model.NewsType.GetDisplayName(): Model.NewsType.ToString())</span>
                </div>
            </div>
        }

        @if (Model.TotalViews > 1000)
        {
            <div class="trending-badge">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
                    <path d="M16 6l2.29 2.29-4.88 4.88-4-4L2 16.59 3.41 18l6-6 4 4 6.3-6.29L22 12V6h-6z" />
                </svg>
                    @(isBangla ? "চলমান" : "Trending")
            </div>
        }
    </div>

    <div class="card-content">
        <header class="card-header">
            <div class="meta-info">
                <time class="publish-time" datetime="@Model.PublishedDate.ToString("yyyy-MM-dd")">
                    @dateText
                </time>
            </div>

            @if (Model.Author != null)
            {
                <div class="author-info">
                    <span class="author-name">@Model.Author.UserName</span>
                </div>
            }
        </header>

        <h3 class="card-title">
            <a href="@Url.Action("Details", "News", new { id = Model.NewsId })"
               class="title-link"
               aria-label="Read full article: @Model.NewsTitle">
                    @Html.Raw(Model.NewsTitle?.RemoveClampBreakingElements().StripHtmlTags())
            </a>
        </h3>

        @if (!string.IsNullOrWhiteSpace(snippet))
        {
            <p class="card-excerpt">
                    @Html.Raw(snippet?.RemoveClampBreakingElements().StripHtmlTags())
            </p>
        }

        <footer class="card-footer">
            <div class="engagement-stats">
                @if (Model.TotalViews > 0)
                {
                    <span class="stat-item views">
                        <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor">
                            <path d="M12 4.5C7 4.5 2.73 7.61 1 12c1.73 4.39 6 7.5 11 7.5s9.27-3.11 11-7.5c-1.73-4.39-6-7.5-11-7.5zM12 17c-2.76 0-5-2.24-5-5s2.24-5 5-5 5 2.24 5 5-2.24 5-5 5zm0-8c-1.66 0-3 1.34-3 3s1.34 3 3 3 3-1.34 3-3-1.34-3-3-3z" />
                        </svg>
                            @(isBangla? Model.TotalViews.ToString().ToBanglaDigits() : Model.TotalViews)
                    </span>
                }
            </div>

            
        </footer>
    </div>
</article>
</a>